# ── URL del backend (FastAPI) ────────────────────────
# Vacío en desarrollo: se usa el proxy de Vite (vite.config.js → /api).
# En producción (Vercel) define la URL pública del backend, p.ej.:
#   VITE_API_URL=https://transitpro-api.onrender.com
VITE_API_URL=
